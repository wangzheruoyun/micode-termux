import type estree from 'estree';
import ts from 'typescript';
/**
 * Detects Cypress assertion calls on the global `cy` chain, e.g.:
 *   cy.get('selector').should('be.visible')
 *   cy.contains('text').and('have.attr', 'href')
 *   cy.intercept(...).as(...); cy.wait('@alias').its('foo').should(...)
 */
export declare function isAssertion(node: estree.Node): boolean;
export declare function isTSAssertion(node: ts.Node): boolean;
export declare function chainStartsWithCy(node: estree.Node): boolean;
