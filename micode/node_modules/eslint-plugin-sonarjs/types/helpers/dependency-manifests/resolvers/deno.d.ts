import type { ManifestResolver } from './types.js';
type ImportMapSpecifier = {
    packageName: string;
    version?: string;
};
export declare const denoManifestResolver: ManifestResolver;
/**
 * Parses an import map URL Specifier matching Deno npm format:
 * npm:<package>[@<version>][/<path>]
 */
export declare function parseInlineNPMImport(value: string): ImportMapSpecifier | undefined;
export {};
